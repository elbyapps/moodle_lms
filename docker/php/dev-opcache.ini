; Dev-only override. Loaded after the entrypoint-rendered opcache.ini
; (alphabetical order: "zz-" sorts last), so these win. Re-enables
; per-request timestamp checks so edits to bind-mounted plugin code are
; picked up immediately instead of being pinned by validate_timestamps=0.
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
